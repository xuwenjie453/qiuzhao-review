# 题目：腐烂的橘子（多源 BFS）

给定 `n × m` 网格：`0` 为空、`1` 为新鲜橘子、`2` 为腐烂橘子。每分钟腐烂向上下左右蔓延。

求所有橘子腐烂的最少分钟数；若仍有橘子无法腐烂，输出 `-1`；没有新鲜橘子时输出 `0`。

输入：第一行 `n m`（均不超过 1000），随后 `n` 行，每行 `m` 个整数。

## 作答

```java
// 在这里完成 Java 实现
```
