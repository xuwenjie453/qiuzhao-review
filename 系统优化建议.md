# 系统优化建议收集箱

> **用途**：收集用户在日常使用中对本学习体系提出的优化建议。
> **约定**：建议只记录、不实现；积累到足够数量后，由用户在**下一次系统迭代**时统一处理。
> Runtime AI 在用户提出建议或发现值得记录的工作流信号时，按下方格式追加条目，并保持"状态"为 `待评估`。

---

## 使用约定（给未来迭代与 Runtime AI）

1. 本文件是**唯一**的建议入口；不得因为某条建议而临时改动正式题库、引擎数据库或 Runtime Prompt。
2. 每条建议必须带真实日期与使用场景，便于迭代时还原上下文。
3. 迭代时的处理顺序建议：先评估高频/阻塞类建议，再做锦上添花类。
4. 迭代完成后，可考虑把"建议收集箱"写入 Runtime 包的 `AGENTS.md`，让每个新 AI 会话都知道它的存在（本次迭代前的 AI 通过用户口述获知）。

---

## 建议列表

| # | 日期 | 场景 | 建议 | 状态 |
|---|------|------|------|------|
| 1 | 2026-09-04 | 算法学习（轮转数组） | 算法题改为"生成 py 答题文件"工作流：题目以注释形式写入文件，ACM 模式（用户自己写输入输出），用户在原文件上作答后交回，AI 实际运行验证。固化此流程，后续算法题默认如此出题。 | 已实施；文件格式后由 #8 细化为 markdown |
| 2 | 2026-09-04 | 系统级 | 建立本建议收集箱，收集使用中的优化建议，攒批后统一迭代。 | 待评估 |
| 3 | 2026-09-04 | 算法学习（轮转数组，迁移验证被跳过） | 算法题完成一道算法（独立实现+测试通过）后该任务即可结束，不需要追加知识迁移验证；对应调整 Algorithms 引擎完成标准（现标准为"实现+测试+边界+一次短迁移验证"）。 | 待评估 |
| 4 | 2026-09-04 | 复习调度 | ①已经特别熟悉的知识点不加入复习队列（入口过滤）；②建立退出机制：复习队列中表现特别熟悉的知识点自动退出队列（出口淘汰），避免复习队列只进不出、膨胀成负担。 | 待评估 |
| 5 | 2026-09-04 | 全程交互体验 | AI 语气改善：更像温柔耐心的老师（鼓励、陪伴、允许慢和出错），而不是严厉的面试官或领导上级（催促、指令式、考核感）。建议固化到 Runtime Prompt 的交互规范中，学习全程适用（含任务布置、纠错、催促环节）。 | 待评估 |
| 6 | 2026-09-04 | 算法验证（生产者-消费者） | 算法题验证从简：写完简单运行一次、通过即收尾，不做压力测试/多轮边界压测，节省时间和 token。AI 附加的深度测试只在用户主动要求或题目本身就考察并发/性能时使用。 | 待评估 |
| 7 | 2026-09-04 | 算法/代码题文件格式 | 代码题的答题文件改为 **markdown 格式**（题目 + 空白代码块），而不是 .py/.java 源文件——避免 VS Code 对源文件自动补全/语法提示的干扰。用户在 md 里作答后交回，AI 提取代码块到临时源文件编译运行。 | 已合并入 #8 |
| 8 | 2026-09-04（晚） | 算法/代码题文件格式（定稿） | 算法题答题文件使用 **markdown 格式**，结构规范：文件以 `#题目` 开头，后跟完整题目（输入输出格式 + 示例）；题目下方明确划分**作答区**，作答区内以**代码块**形式作答。AI 收到后提取代码块到临时源文件编译运行验证。不生成 .py/.java 源文件。 | **已实施**（已写入 Algorithms Runtime Prompt 与 AGENTS.md，用户授权本次迭代） |
| 9 | 2026-09-04（晚） | 调度偏好 | **算法题推送频率减少，知识点推送频率增加**：算法题费脑子，减少认知消耗。默认正常学习日 Algorithms 至多 1～2 题，其余任务优先 Knowledge；用户当天主动要求时临时加量（session 级）。 | **已实施**（已写入 Temporal Scheduler Prompt 与 AGENTS.md，用户授权本次迭代） |
| 10 | 2026-09-05（晨） | 复习机制 | ~~脆弱点独立成胶囊~~ **已撤销**：用户评估后认为现行规则（脆弱点记录在原胶囊 known_failure_modes 中随原周期复习）已足够，不引入独立修复胶囊。 | 已撤销（用户自行决定） |

---

*(新建议追加在表格下方，保持编号递增)*
