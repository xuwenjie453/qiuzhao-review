# 双端更新实施状态

## 当前阶段
全部完成：Phase0–Phase7（Phase3–5 真机类验收项标记 NOT_RUN / BLOCKED_BY_MISSING_INPUT）。

## 已完成
- Phase0：契约冻结（目录骨架、fixtures、实施状态文档、gitignore）。
- Phase1 MacGraphCore：`DualEnd-Mac` store(StateDb)+graph service+validators —— 16/16 unit 绿。
- Phase2 Bridge：envelope/ws-server/bonjour/sync-session/local-control/daemon/CLI —— bridge E2E 7/7 绿；daemon 真实启动冒烟（READY/Bonjour/锁/停止）通过；协议 fixtures 契约测试 7/7。
- Phase3–5 iPad：完整 Xcode 工程（QiuZhaoReader.xcodeproj，含 unit test target）；Domain/Protocol/Connectivity(Bonjour+URLSession WS)/Sync/Store(actor SQLite)/Topology/Reader(单坐标系 Markdown+Ink)/Pencil(App) 全部就位 —— BUILD SUCCEEDED，单元测试 12/12 绿。
- Phase6：RuntimePrompt `21_双端问题图与iPad协作协议.md`、AGENTS/README 双端章节、根目录说明。
- Phase7：`双端Release验收清单.md` A–G 逐项状态。

## 测试证据
- `cd DualEnd-Mac && node --test test/` → 16 unit + 7 bridge E2E + 7 fixtures = 30 PASS / 0 FAIL
- `python3 学习系统/test_e2e.py` → 21 PASS（原系统未破坏）
- `xcodebuild -project QiuZhaoReader.xcodeproj -scheme QiuZhaoReader -destination 'generic/platform=iOS Simulator' build` → BUILD SUCCEEDED
- `xcodebuild ... -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5)' test` → 12 PASS / 0 FAIL

## 阻塞 / 未决
- 真机 iPad + Apple Pencil Gate：NOT_RUN（无真机）。对应验收项已明确标注。
- 参考 PDF typography：BLOCKED_BY_MISSING_INPUT（ReaderTypography 数值未冻结）。
- Swift 测试 fixture 与 DualEnd-common 未做 bundle 同源（样例 JSON 内容一致）。

## 已确认的不变量
见 `双端Release验收清单.md` 与冻结产品规则（CENTER 不可删、body immutable、Temporary 轮次隔离、durable-before-ACK/emit、零配对等全部以测试固化）。
