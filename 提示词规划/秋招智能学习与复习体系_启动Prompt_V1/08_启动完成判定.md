# 启动完成判定

全部满足才向用户宣布"系统已就绪"；任何一项不满足，如实说明哪部分可用、哪部分待处理。

## 学习核心（必查）

- [ ] 真实日期已获取；
- [ ] `cli.py status` 输出正常（ACTIVE Goal 存在；无 Goal 时已按流程提示用户）；
- [ ] `cli.py integrity` 通过。

## 双端（按需，未启动不算失败）

- [ ] daemon `state=READY`；
- [ ] iPad 徽章"已连接"、`sessions ≥ 1`；
- [ ] `graph snapshot` 正常；30 秒人工冒烟通过（双击进 Reader → 写一笔 → 重进笔迹在位）。

## 收尾输出（给用户的一句话形态，不展示内部细节）

> 系统已就绪：学习核心正常（当前目标 25 天覆盖冲刺进行中）[；双端已连接，iPad 可用]。说"开始学习"即可进入今日任务。

## 注意

- 未启动 daemon/iPad 时改为："双端未启动（可选）——需要时运行 `node DualEnd-Mac/bin/qreview-dual.mjs daemon start`。学习不受影响。"
- 启动过程产生的一切异常与处置，如实记录在回复中；不落学习事件库。
