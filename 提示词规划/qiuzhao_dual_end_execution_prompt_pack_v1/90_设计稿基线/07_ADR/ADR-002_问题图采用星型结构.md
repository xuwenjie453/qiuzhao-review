# ADR-002：问题图 v1 采用中心辐射星型

Status: Accepted

## Decision

QuestionGraph 语义只表达“这些解释属于这个问题”。不设计 explanation 之间的边。

## Rationale

需求只定义中心问题和若干解释节点，没有定义解释之间的语义关系。让 AI 自动建边会重新引入“系统替用户做知识结构判断”。

## Future

若用户未来明确提出边/分组，可 protocol v2 增加；v1 不预埋可编辑关系 UI。
