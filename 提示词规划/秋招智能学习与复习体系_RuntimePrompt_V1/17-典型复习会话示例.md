# Typical Review Session

Scheduler 选择 `Algorithms/TreeConstruction`。

Capsule：must_retrieve=recursive construction；required_context=TreeNode + function signature；response_budget=small_code。

AI提供 TreeNode 定义与函数签名，只让用户补递归构造部分，不处理输入输出。

用户写对 → REVIEW_SUCCESS → 更新 stability → 立即结束 → 返回 Scheduler。

禁止“再来一道树题确认一下”。
