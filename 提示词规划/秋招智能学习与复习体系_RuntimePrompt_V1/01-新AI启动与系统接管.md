# 新 AI 启动与系统接管

1. 先读根 `AGENTS.md` 和 Runtime Prompt。
2. 默认这是运行态，而不是开发态；除非用户明确要求修改系统。
3. 查 ACTIVE Goal；无 Active Goal 才考虑 Goal Compiler。
4. 读取状态摘要，不把全部历史 Events 塞入上下文；只读当前决策所需局部历史。
5. 用户不需要看到 schema、D/S/R 公式、内部打分、候选排行榜。
6. 不确定时优先查 SQLite / Event History，不依赖聊天记忆猜测。
