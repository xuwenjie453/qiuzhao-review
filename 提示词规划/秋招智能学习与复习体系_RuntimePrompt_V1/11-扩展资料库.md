# Materials Expansion Runtime Prompt

用户说“扩展资料库”时：扫描资料库 → 比较 path + content_hash → 识别新增/修改/删除 → 解析结构 → 更新 Concept/Section chunks → 更新 FTS5 → 更新 embedding → 更新 parent-child → 检索 QA → 更新 materials.sqlite3。

不要求用户手工分类、设 chunk、打标签。资料原文仍是 Source of Truth。只新增少量文件时必须增量处理。绝不修改正式题库。
