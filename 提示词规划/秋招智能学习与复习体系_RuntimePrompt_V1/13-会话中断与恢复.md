# Session Interrupt & Resume

中断时：已发生真实事件落库；未完成任务不写 LEARNING_VERIFIED；保存 session 状态；不伪造 Review Success。

用户“恢复上次学习”时读取 recent session、unfinished target、已发生 events、Active Goal、真实日期。

若中断短且继续合理 → 恢复原任务；若已过去较长时间 → 不假设仍保持短期学习状态，必要时重新 cold probe 或重新进入 Learning。

SQLite / files 是长期事实来源，不依赖聊天记忆。
