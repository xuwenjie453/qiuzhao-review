# Master Prompt：执行 Pencil 悬停预览修复实施 v3

你负责按本目录提示词包修复 iPad Reader 的 Apple Pencil 近屏实时渲染错位。根因已冻结（`02`），设计已冻结（`02`），你的任务是**按阶段实施并守闸门**，不是重新设计。

## 目标

1. Pencil 近屏（悬停或书写中）时，悬停预览与书写中墨迹都出现在笔尖正下方，错位消失。
2. “Pencil 远离后笔迹归位”现象消失（因为没有错位可归位）。
3. 实际落笔位置与修复前的 canonical 坐标一致（历史笔迹零迁移、重开对齐）。
4. 双击节点进入 Reader 的链路不回归（这是上一轮重构翻车点，必须有测试闸门）。
5. 旋转、多页、退出重进、后台 flush、双击切橡皮全部保持可用。

## 必须阅读（按序）

- 根 `AGENTS.md`（工程任务纪律：只在用户明确要求时动代码——本包即用户要求）
- `提示词规划/Pencil悬停预览根因修复_v2/`（前序诊断契约，特别是 09/10/11/13 号）
- 本包 `02`（根因与设计冻结）、`03`（范围与安全）
- 源码：`DualEnd-iPad/QiuZhaoReader/Reader/AnnotatedReaderView.swift`、`ReaderHostView.swift`、`ReaderTypography.swift`、`Pencil/PencilToolController.swift`、`App/AppSessionModel.swift`、`Store/ClientStore.swift`
- `git show 5121d17 -- <file>` 查看「暂存」参考实现（结合 `06` 修正清单使用）

## 阶段闸门（硬性，不得跳过或合并）

```text
Phase 0 真机取证 ──(证据支持根因？)──→ Phase 1 架构实施
                     │ 否 → 停止，报告证据，等待用户决策
Phase 1 ──→ Phase 2 回归加固 ──(模拟器测试全绿？)──→ Phase 3 真机 QA
                              │ 否 → 修复测试，禁止装机
Phase 3 ──(真机逐项通过？)──→ 11 号复核交付
```

- Phase 0 在**旧架构基线**上加只读探针构建，测量数据支持冻结的根因后才动架构。
- Phase 1/2 期间任何时刻 `DualEnd-Mac`、学习系统、协议文件必须零改动。
- Phase 3 未完成前，任何输出不得声称“已修复”。

## 执行纪律

- 每阶段开始先输出：本阶段将做什么、验收标准是什么；结束输出证据（命令 + 结果）。
- 一次只改一层：探针构建、架构实施、测试加固分别独立 commit，失败只回退当前层。
- 所有几何计算基于实际 view 的 bounds；A4 canonical 尺寸 `595.92 × 842.88` 不变。
- 每处代码改动给出矩阵层面的理由（引用 `02` 的不变量公式）。
- 不确定 PencilKit 私有行为时，用探针/测试取证，不靠记忆断言。

## 完成条件（全部满足才可关闭本包）

1. `07` 纯逻辑单测、`08` 全链路冒烟、既有 21 项测试全部通过（模拟器）。
2. `10` 真机 QA 清单逐项有证据，特别是历史笔迹兼容与近屏贴合。
3. `11` 交付报告完成，含修改清单、矩阵修复前后对照、测试结果、真机证据、剩余风险。
4. 工作分支合入与否由用户决定；`main` 位置不得擅动。
