### ProjectGeneral-001-001 | ★★★★☆

项目里核心表的表结构是怎么设计的？为什么这么设计？

**答案：**
核心实体与关系 → 关键设计决策（主键、类型、冗余字段、冷热分离）→ 每个决策的"为什么" → 演进（加字段/拆表的历史）。

- 主键选型理由（自增 vs 雪花，关联索引结构）
- 冗余有据："为列表页免 join 冗余了 X，一致性问题靠 Y 兜底"
- 状态字段用 tinyint + 字典表约束，金额用 DECIMAL，时间统一 UTC

**解析：**
- 字段类型/范式/冗余的权衡是否理性，而不是照抄模板
- 是否为查询模式而设计（表结构服务 SQL）


### ProjectGeneral-001-002 | ★★★★☆

项目的索引是怎么设计的？做过哪些 SQL 优化？

**答案：**
列高频查询模式 → 索引设计方法（等值/范围/排序四段法）→ 具体优化案例（before/after）→ 防退化机制（SQL 审核卡点、慢日志监控）。

- 一个完整案例：慢 SQL → explain 分析（type/rows/Extra）→ 改写或加联合索引 → 效果量化
- 覆盖索引消除回表的真实用例
- 说出索引的成本意识（写放大、空间）

**解析：**
- 是否按查询模式建索引（最左前缀/覆盖索引的实战运用）
- 有没有"上线前验证"的工程习惯


### ProjectGeneral-001-003 | ★★★★☆

数据量涨到多大会成为问题？你们怎么规划容量与归档？

**答案：**
量级推演（当前增速 → 何时到瓶颈：B+ 树层级/缓冲池命中率/备份窗口）→ 应对阶梯（索引优化→读写分离→归档→分库分表）→ 归档实践（方案、迁移一致性、查询兼容）。

- 能推演：给出增长率与单行大小，算出到瓶颈的时间
- 归档不是删库：冷数据可查（归档库/对象存储），查询路由兼容
- 明确"分库分表是最后手段"及理由

**解析：**
- 容量规划的前瞻性（而不是等慢了再说）
- 冷热分离/归档的工程实践


### ProjectGeneral-001-004 | ★★★★☆

你的表如果查询模式变了（新需求按 B 字段查），索引怎么演进？

**答案：**
新查询画像（选择性/频率/组合）→ 按四段法设计新联合索引 → 评估对写入的影响（写放大）→ 在线加索引（低峰/gh-ost）→ 用 sys.schema_unused_indexes 观察旧索引使用率再决定下线。

说出"索引有生命周期"（上-监控-下线）；新索引与旧索引的兼容/冗余判断（(a,b) 已覆盖 a 单列）。

**解析：**
索引不是一次性设计而是演进治理；变更安全意识。


### ProjectGeneral-001-005 | ★★★★☆

项目里哪些接口做了幂等？怎么实现的？

**答案：**
识别哪些操作需要幂等（重试路径上的所有写操作）→ 分层实现（入口 token/存储唯一键/状态机）→ 并发场景验证（压测并发重复请求）→ 冲突后的语义处理（返回"已成功"）。

- 幂等键的设计（客户端 requestId vs 业务单号）与全链路透传
- Redis 前置 + DB 唯一键兜底的双层结构
- 唯一键冲突时返回幂等成功语义而非报错

**解析：**
- 幂等是"背了概念"还是"按层落地"（token/唯一键/状态机的组合）
- 并发窗口是否真的堵住（DB 唯一键兜底）


### ProjectGeneral-001-006 | ★★★★☆

项目里跨服务的数据一致性怎么保证的？

**答案：**
列出跨服务一致性场景 → 每个场景的方案选型（消息最终一致/TCC/SAGA）与理由 → 异常路径（消息失败/补偿失败）→ 对账与人工通道。

- 按业务分级：资金用强一致或 TCC，联动展示用消息最终一致
- 说得出补偿失败的第三层：告警+人工工单
- 对账的比对口径与频率

**解析：**
- 一致性方案的选型论证（强一致 vs 最终一致的业务依据）
- 兜底完整性（补偿、对账、人工通道）


### ProjectGeneral-001-007 | ★★★★☆

对账系统在项目里是怎么设计的？（如无可答"如何设计一个"）

**答案：**
对账范围与频率 → 数据准备（双方账单拉取/文件校验）→ 核对算法（精确匹配/模糊窗/在途单）→ 差异分级处理（自动补单/人工工单）→ 审计留痕。

- 文件级校验先行（笔数+金额合计），防漏对
- 差异分类：单边/金额不符/状态不符，各有处理流
- 对账任务幂等可重跑（K-DIST-009 呼应）

**解析：**
- 最终一致体系的闭环意识（对账是最后防线）
- 差错处理的工程化程度（自动化分级 vs 全靠人工）


### ProjectGeneral-001-008 | ★★★★☆

你说接口做了幂等，那"重试安全的接口"和"幂等接口"是一回事吗？

**答案：**
定义对齐（幂等=多次执行效果等同一次）→ 三类接口盘点（天然幂等：查询/删除/条件更新；非幂等：无键插入/叠加扣减；改造点：幂等键+唯一约束/状态机）→ 重试策略与幂等的匹配（非幂等接口禁自动重试或改造成幂等后再放开）→ 验证（并发重放测试）。

说出"重试的前提是幂等"这条工程铁律；举一个从非幂等改造为幂等的具体接口（加 requestId 唯一索引）。

**解析：**
幂等语义的精确定义；与重试机制的配合边界。


### ProjectGeneral-001-009 | ★★★★☆

请用 1~2 分钟介绍一下你的项目

**答案：**
背景（业务问题与规模）→ 我的角色 → 架构一句话 → 2~3 个技术亮点（每个对应一个难点/成果）→ 结果（量化）→ 引导句（"这里面我最熟悉的是 XX，可以展开"）。

- 30 秒内说清"这个项目解决什么问题、服务多少人/多少请求"
- 亮点与难点挂钩（"难点是 A，我用 B 方案解决"）
- 结尾主动抛钩子，掌握追问方向

**解析：**
- 表达是否结构化、有重点；是否清楚项目的业务价值而非只会堆技术词
- 5 分钟内判断你的参与深度（是 owner 还是打杂）
- 埋下后续追问的钩子——好的自我介绍会主动引导面试官问你最擅长的部分


### ProjectGeneral-001-010 | ★★★★☆

你在项目里承担什么职责？

**答案：**
职责范围（模块/服务清单）→ 关键决策中我的角色（主导/参与/执行）→ 与他人协作边界 → 产出物（代码量级不必说，说交付了什么能力/文档/规范）。

- 诚实分级："整体架构是团队讨论的，其中 X 方案是我提出并落地的"
- 说得出协作接口："我依赖算法组的召回接口，对齐了字段契约"
- 有带人/评审/规范类产出是加分项

**解析：**
- 区分"主导设计"与"参与执行"，评估职级匹配度
- 校验项目真实性：真做过的人对分工细节脱口而出


### ProjectGeneral-001-011 | ★★★★☆

项目里遇到的最大技术难点是什么？怎么解决的？

**答案：**
STAR-L：背景与现象（什么问题，影响多大）→ 定位过程（怎么缩小范围的，用了什么工具/数据）→ 方案对比（至少两个候选+取舍理由）→ 落地与验证（灰度？指标对比？）→ 复盘沉淀（规则/工具/文档防复发）。

- 定位过程有"证据链"（日志/监控/压测数据），而不是"试出来的"
- 方案有取舍（"考虑过 A，因为 X 放弃"）
- 有防复发沉淀（这是资深与初级的分水岭）

**解析：**
- 这是最重要的项目题：考察"发现问题→分析→方案→验证"的完整闭环能力
- 判断难点的含金量（是搜索出来的 bug 还是真正的工程权衡）


### ProjectGeneral-001-012 | ★★★★☆

面试官怎么判断项目是不是你真实做的？你会怎么证明？

**答案：**
真实性体现在细节的密度：数据来源、踩坑记录、版本演进、被否决的方案。主动展示三层下钻能力：架构层 → 设计层 → 代码/配置层（关键参数、SQL、边界条件）。

- 能说出"当时为什么不用另一个方案"（被否决的历史）
- 能说出具体的坑（如某个中间件版本的行为差异）
- 数据有出处（压测报告/监控截图意识）

**解析：**
- 反向提示你：以下这些点会被用来验证真实性——准备时就该自查
- 考察你对自己项目的"细节颗粒度"（真做过的人能下钻三层）


### ProjectGeneral-001-013 | ★★★★☆

你的项目数据量/流量到底多大？怎么证明你说的是真的？

**答案：**
分层数字——入口流量（QPS/日请求）、数据规模（表行数/存储量/文档数）、链路指标（P95/成功率）、资源水位（实例数/CPU 均值）。数字给量级+出处（监控截图/压测报告/容量评审），主动说"精确值以【监控平台】为准"。

口径准确（峰值 vs 均值分得清）；能现场推演（日活×人均请求≈QPS）；对不熟的部分诚实说"这块是【同事/上游】负责，我记得量级是【X】"。

**解析：**
校验项目真实性；量化意识（无数字=没做过或没思考）。


### ProjectGeneral-001-014 | ★★★★☆

项目里的事务是怎么设计的？哪些地方踩过事务的坑？

**答案：**
事务边界划定原则（一个用例一个事务，远程调用移出事务）→ 传播行为用例 → 踩过的坑与修复 → 大事务治理。

- 明确"事务内不发 RPC/不发 MQ"的原则及替代方案（事务提交后执行/本地消息表）
- 能讲一个具体的失效案例（自调用/rollbackFor/吞异常）
- 批处理用 TransactionTemplate 分批提交

**解析：**
- @Transactional 是否真的会用（失效场景是高频埋雷点）
- 大事务意识与边界设计能力


### ProjectGeneral-001-015 | ★★★★☆

项目里线程池是怎么配置的？为什么这么配？

**答案：**
有哪些异步场景 → 每个池的参数与依据（任务特征+压测）→ 队列与拒绝策略 → 监控与动态调参。

- 区分 IO 密集/CPU 密集并给出参数差异
- 有界队列+明确拒绝策略，说出拒绝后的行为
- 提到动态线程池/监控（活跃度、队列深度告警）

**解析：**
- 是拍脑袋 core=8 max=16，还是理解任务特征与压测校准
- 隔离与拒绝策略的实战意识


### ProjectGeneral-001-016 | ★★★★☆

项目里接口设计的规范和取舍？(幂等/版本/契约)

**答案：**
接口分层（内部 RPC/对外 OpenAPI/前端 BFF）→ 各层规范重点（幂等键、错误码结构、版本策略、限流配额）→ 一个具体的接口设计案例。

- 写接口必有幂等设计（token/唯一键/状态机）
- 错误码体系：HTTP 语义 + 业务码分段 + 可追溯
- 版本演进：兼容性原则（只加不改删）、灰度双跑

**解析：**
- 契约意识：幂等、错误码、版本演进是否系统性思考过
- 开放接口与内部接口的区别认知


### ProjectGeneral-001-017 | ★★★★☆

项目里为什么引入 MQ？用在哪条链路上？

**答案：**
同步链路的痛点（RT 长/耦合/峰值扛不住）→ 拆分：哪些留同步、哪些异步化 → 选型（对比过的 MQ 与理由）→ 收益与新增保障（幂等、监控）。

- 明确"主链路只保留强一致动作，其余事件化"
- 选型有对比（如"要事务消息和延迟消息选了 RocketMQ 而不是 Kafka"）
- 说出异步后的用户语义处理（前端提示"已受理"）

**解析：**
- 异步化决策是否基于业务分析（哪些环节适合异步、哪些绝不能）
- 引入后主链路语义的变化是否清楚（"受理成功"≠"完成"）


### ProjectGeneral-001-018 | ★★★★☆

消息不丢失在项目里是怎么落地的？

**答案：**
按链路三段列配置 → 每段一个真实细节（如 acks=all 的 min.insync.replicas 值、手动 ACK 位点逻辑）→ 对账兜底 → 发现过的一次丢失风险案例（如有）。

- 生产端：发送确认+失败重试+本地消息表（业务成功则消息必达）
- 消费端：手动提交位点的具体实现（处理成功才 commit）
- 对账任务的存在及其比对口径

**解析：**
- 三段式（生产/存储/消费）是否有具体配置而非空话
- 有没有"最后一道闸"（对账）的意识


### ProjectGeneral-001-019 | ★★★★☆

消费端的幂等和顺序是怎么保证的？

**答案：**
哪些消息需要顺序（按业务实体论证）→ 实现三件套（key 路由/分区 FIFO/消费端串行）→ 幂等实现（状态机+唯一键）→ 重试与顺序的冲突处理。

- 明确局部顺序（同一订单 key）而非全局顺序
- 幂等的具体实现：条件更新 `where status='待支付'`
- 消费失败时不跳过该 key 后续消息的处理

**解析：**
- 幂等落地层次（DB 唯一键是硬底，别只说 Redis）
- 顺序是否真的必要（为不必要的需求背全局顺序代价是减分）


### ProjectGeneral-001-020 | ★★★★☆

消费延迟（Lag）你们怎么告警和处置的？

**答案：**
Lag 指标采集（消费位点-写入位点）→ 分级告警（Lag>【X】持续【Y】分钟 P1；消息年龄>【Z】秒 P0——年龄比条数更能反映业务影响）→ 处置预案（扩消费者/定位慢消费/降级非核心消费/必要时清理重放）。

区分"条数积压"与"消息年龄"；说出一次真实处置（根因：下游慢 SQL/单条消息处理重）；知道消费者数≤分区数的天花板。

**解析：**
MQ 运维化成熟度；积压的发现-处置闭环。


### ProjectGeneral-001-021 | ★★★★☆

你们的服务是怎么拆的？为什么这么拆？

**答案：**
按业务能力列出服务清单与职责 → 拆分依据（领域边界/变更频率/团队归属）→ 数据归属原则 → 没拆的部分与理由 → 拆分后的代价与治理（调用链、事务）。

- 数据库私有（database per service），跨域只走接口/事件
- 有"暂时不拆"的克制理由（模块化单体优先）
- 拆分后新增问题的应对（分布式事务→消息最终一致、链路追踪）

**解析：**
- 拆分依据是业务域还是技术分层（后者是反模式）
- 是否理解拆分的代价，有没有"拆过头"的自省


### ProjectGeneral-001-022 | ★★★★☆

服务间调用的稳定性怎么保障的？（超时/重试/熔断/降级）

**答案：**
依赖分级（核心/重要/边缘）→ 每级的超时重试策略（幂等前提）→ 熔断规则与半开验证 → 降级预案（给什么兜底）→ 演练机制（故障注入/预案 review）。

- 超时预算沿链路递减的具体数值设计
- 重试有幂等前提与重试预算（防放大）
- 降级方案具体到"返回什么"（缓存/默认值/简化流程），且演练过

**解析：**
- 四件套是"配了参数"还是"有体系"（预算、分级、演练）
- 降级预案是否真的演练过


### ProjectGeneral-001-023 | ★★★★☆

服务的发布流程和灰度策略？

**答案：**
标准流水线（CI 门禁→测试→预发→生产灰度→全量）→ 灰度维度（实例/流量/用户标签）→ 兼容性规范（接口只增不改、DB 变更先加后删、配置与代码顺序）→ 回滚预案与演练。

- DB 变更的兼容三步（加字段→双写/迁移→删旧字段跨版本）
- 灰度期间看什么指标判定放行（错误率/RT/业务指标）
- 回滚的自动化程度与数据回滚难题的预案

**解析：**
- 发布安全意识（回滚、灰度、兼容性）
- 全链路视角：数据库变更/配置变更也属于发布


### ProjectGeneral-001-024 | ★★★★☆

服务依赖梳理：强依赖挂了你们会怎样？

**答案：**
依赖盘点方法（调用链拓扑+变更频率）→ 分级标准（强依赖：挂了主流程不可用，如库存/支付；弱依赖：可降级，如推荐/评论）→ 每级的动作（强依赖熔断保护+多活/重试；弱依赖开关一键降级+异步化）→ 演练证据。

能背出自己服务的强依赖清单；演练发现的坑（开关没生效/降级返回引发前端异常）。

**解析：**
强弱依赖分级与预案的真实落地。


### ProjectGeneral-001-025 | ★★★★☆

项目的发布流程和 CI/CD 管线是什么样的？

**答案：**
管线阶段（构建→静态检查→单测门禁→镜像→环境晋升）→ 每阶段的门禁标准 → 灰度与回滚 → 提效措施（缓存、并行）。

- 说得出具体门禁阈值（覆盖率/漏洞等级）与失败后果
- 镜像构建的最佳实践落地（多阶段/层缓存，K-TOOL-005）
- 回滚的实测时长

**解析：**
- 是否理解"流水线是质量门禁"而不只是自动打包
- 环境晋升与回滚的实际操作能力


### ProjectGeneral-001-026 | ★★★★☆

项目容器化/K8s 落地做了什么？

**答案：**
容器化改造点（Dockerfile/基础镜像/配置外置）→ K8s 资源设计（requests/limits、探针、优雅停机）→ 踩坑案例（OOMKilled/停机丢请求）→ 效果。

- JVM 参数与容器 limit 匹配（MaxRAMPercentage，K-JVM-011）
- preStop + readiness 优雅停机链路（先摘流量再杀进程）
- 探针配置依据（liveness 不能依赖下游）

**解析：**
- Java 上容器的特有坑（资源感知、优雅停机、JVM 参数）是否踩过
- 声明式发布与探针的正确配置


### ProjectGeneral-001-027 | ★★★★☆

日志、指标、链路追踪三件套在项目里怎么落地的？

**答案：**
三件套选型与部署 → traceId 全链路贯穿（HTTP/MQ/异步）→ 日志规范与 MDC → 典型排障案例走一遍（一个 trace 定位问题）。

- 跨异步边界（线程池/MQ）的上下文桥接（K-JUC-008 的 TTL 方案）
- 日志带 traceId 可按 trace 聚合，指标可按版本/维度下钻
- 一个真实排障故事：从告警→trace→日志→根因

**解析：**
- 可观测是"排障时三轴联动"的体系，不是三套孤立工具
- traceId 贯穿与上下文传递的工程细节


### ProjectGeneral-001-028 | ★★★★☆

项目里做过哪些性能优化？怎么定位瓶颈的？

**答案：**
性能基线与目标（什么指标、多少算达标）→ 定位（分层压测/监控下钻/工具证据）→ 优化清单（按收益排序）→ 效果验证（before/after 数据）→ 回归保障。

- 有明确的分析过程：请求耗时分解（网关/服务/DB/下游），找到占比最大的一层
- 优化手段有取舍（"缓存能解决但引入一致性成本，先做了 SQL 改写"）
- 效果用数字说话且经过复测

**解析：**
- 优化是否"先测量后动手"（没有基线的优化是盲改）
- 优化手段的层次感（代码→缓存→架构，而不是一上来加机器）


### ProjectGeneral-001-029 | ★★★★☆

系统的容量上限怎么评估的？（QPS 推演）

**答案：**
理论推演（单机瓶颈：线程/连接/带宽 → 全链路短板=最小容量）→ 压测方案（环境数据量级、梯度、指标）→ 结果与扩容策略（什么时候加机器/分库）→ 日常水位管理。

- 找"最短木板"：DB 连接数/下游配额往往先于 CPU 到顶
- 压测细节：数据真实性（热身缓存）、监控对比生产、防止压垮生产
- 给出安全水位（如 60%）与弹性预案

**解析：**
- 是否有"从单组件容量推全链路"的系统思维
- 压测方法论（环境、数据、梯度、拐点）


### ProjectGeneral-001-030 | ★★★★☆

高并发下数据库成为瓶颈怎么办？（按你的项目答）

**答案：**
判断瓶颈类型（读多→读写分离+缓存；写多→归档/分片/异构）→ 已做的阶梯 → 效果与代价 → 下一阶段的预案。

- 顺序感：索引/SQL → 缓存 → 读写分离 → 归档冷数据 → 分库分表
- 读写分离的写后读问题及方案（强制读主/GTID 等待）
- 分库分表触发条件明确（不提前做）

**解析：**
- 优化阶梯的完整认知（先榨单机再扩展）与每个阶梯的代价
- 读写分离后的读一致性问题是否踩过


### ProjectGeneral-001-031 | ★★★★☆

讲一个"压测结果和线上表现对不上"的经历（或怎么避免）

**答案：**
差异来源清单——数据分布（压测随机 vs 线上热 key 集中）、缓存状态（冷压 vs 线上热）、环境（规格/数据量/共享邻居）、流量模型（平推 vs 突刺）→ 对策（影子库真实数据量、按线上访问分布造数、预热后测量、生产环境只做只读接口低峰探测）。

承认压测天然失真，讲"如何缩小差距"而不是吹完美；给出对不上之后怎么修正模型的故事。

**解析：**
压测方法论素养（数据模型/预热/环境差异）。


### ProjectGeneral-001-032 | ★★★★☆

项目里为什么引入 Redis？不用行不行？

**答案：**
先说没有缓存时的具体问题（量化：DB 压力/接口延迟）→ 哪些数据适合缓存（读多写少/可容忍短暂旧值/可重建）→ 引入后的收益（量化）→ 为此付出的代价与应对（一致性方案、运维）。

- 有"不用行不行"的正面回答：哪些读路径其实没走缓存、为什么
- 数据特征清晰：命中率【X%】、更新频率
- 代价诚实：为一致性做了什么（K-REDIS-007 的方案）

**解析：**
- 技术选型是否有"问题驱动"（而不是简历驱动）
- 是否清楚缓存引入的代价（一致性、复杂度、成本）


### ProjectGeneral-001-033 | ★★★★☆

缓存和数据库的一致性方案是怎么定的？

**答案：**
选型论证（先删后更 vs 先更后删 vs 更新缓存）→ 残留风险与时序分析 → 三层兜底（删除重试/binlog 订阅/对账）→ 一致性级别的诚实声明（最终一致）。

- 能画出"先删后更"的并发坏时序，论证为什么弃用
- 说得出删除失败的兜底实现（重试队列 or Canal 订阅）
- 明确业务可接受的不一致窗口（如秒级），超出即告警

**解析：**
- 是背"先更库再删缓存"的结论，还是理解时序与并发窗口
- 兜底链路完整性（重试、订阅、对账）


### ProjectGeneral-001-034 | ★★★★☆

项目遇到过缓存穿透/击穿/雪崩吗？怎么防的？

**答案：**
分别对应项目的真实风险面（恶意 ID 扫描→穿透；爆款商品过期→击穿；大促整点过期→雪崩）→ 各自落地的方案 → 防护的验证方式。

- 穿透：布隆过滤器/空值缓存的选型与参数（误判率、TTL）
- 击穿：互斥重建或逻辑过期，说明为何这么选（一致性 vs 可用性）
- 雪崩：TTL 加抖动 + 多级缓存 + DB 保护（限流）

**解析：**
- 三者是否分得清，方案是否真的落地过（而不是背八股）
- 防护的"度"：过度防护也是成本


### ProjectGeneral-001-035 | ★★★★☆

分布式锁在项目里怎么用的？踩过什么坑？

**答案：**
使用场景 → 实现选型（Redisson 的理由）→ 关键配置（超时/看门狗/等待策略）→ 踩坑案例 → 与幂等/DB 的组合关系。

- 说得出"锁不是正确性保证只是性能优化"，真正兜底是唯一约束/状态机
- 踩坑真实案例（如任务超时锁过期导致双跑，加看门狗后解决）
- 等待策略（快速失败 vs 自旋等待）按场景选择

**解析：**
- 锁的三要素（互斥/防死锁/防误删）是否有实战理解
- 锁的边界：什么场景必须配合 DB 兜底


### ProjectGeneral-001-036 | ★★★★☆

缓存的读命中率和回源率你们监控吗？数据说明什么？

**答案：**
指标定义（命中率=命中/请求；回源 QPS）→ 当前值与水位（命中率【X%】、回源峰值【Y】）→ 低于预期的归因案例（TTL 太短/冷数据多/key 设计散）→ 优化动作与效果。

能讲一个"命中率下降排查"故事（如某次 key 前缀改版导致缓存失效风暴）；知道命中率不是越高越好（过高可能意味着脏数据容忍过大）。

**解析：**
缓存是"数据驱动的系统"不是装完就完；命中率经济学。


### ProjectGeneral-001-037 | ★★★★☆

项目的认证授权体系是怎么设计的？

**答案：**
认证方案（Session vs JWT 及理由）→ 授权模型（RBAC/数据权限）→ 关键安全属性（Token 生命周期、刷新、吊销、存储位置）→ 一个具体链路（登录→访问→登出）。

- 选型有业务依据："需要强制下线能力所以选 Session/短 JWT+黑名单"
- 授权分功能权限（RBAC）与数据权限（行级过滤）两层
- Cookie 安全属性（HttpOnly/Secure/SameSite）或 Token 存储的 XSS 权衡讲得清

**解析：**
- 认证（你是谁）与授权（你能干什么）是否分层清晰
- 会话/令牌的选型依据与安全属性（可吊销性）


### ProjectGeneral-001-038 | ★★★★☆

项目的接口安全和数据安全做了哪些事？

**答案：**
接口层（TLS/HMAC 签名/时间戳 nonce/限流）→ 数据层（传输/存储/展示三态：字段加密、盲索引、脱敏规则）→ 日志与审计（红线清单、留痕）→ 合规动作（等保/审计对接，视行业）。

- 签名覆盖范围具体（哪些字段、排序规则）且两端联调踩过坑
- 密码 bcrypt、卡号/手机号的存储加密+展示掩码+日志红线
- 审计日志不可篡改、保留时长有说法

**解析：**
- 接口三件套（认证/签名/防重放）与数据全生命周期安全
- 合规意识（脱敏、审计、日志红线）


### ProjectGeneral-001-039 | ★★★★☆

数据权限（行级权限）是怎么实现的？

**答案：**
权限模型（部门树/角色可见范围/密级）→ 注入机制（统一拦截：MyBatis 拦截器/AOP 改写 SQL，禁手写 where）→ 性能（路径前缀索引、缓存权限位）→ 测试与审计（越权用例、权限变更生效时间）。

- 强制注入而非依赖每个开发者自觉（框架级保障）
- 部门树用路径枚举前缀匹配，索引友好
- 权限变更的实时性（缓存失效策略）与越权测试用例

**解析：**
- 数据权限是"前端隐藏按钮"还是"SQL 级强制过滤"（安全成熟度）
- 性能与正确性的平衡


### ProjectGeneral-001-040 | ★★★★☆

一次安全扫描/渗透测试发现的整改经历

**答案：**
发现渠道（SAST/DAST/渗透/众测）→ 问题分类（注入/越权/敏感信息/依赖漏洞）→ 整改方案与取舍（修复成本 vs 风险）→ 回归验证与防复发（CI 门禁/规则入库）。

讲具体问题（如"日志打印完整手机号被合规扫描点名"）与系统性修复（统一脱敏工具类+CR 规则），而非单点应付。

**解析：**
安全是"被发现-整改-回归"的工程闭环；合规敏感度。


### ProjectGeneral-001-041 | ★★★★☆

讲一次你处理过的线上故障（P0/P1 级）

**答案：**
影响面与时间线（发现→响应→止血→恢复，各节点时刻）→ 止血动作（优先恢复不是找根因）→ 根因定位（证据链）→ 修复 → 复盘产出（监控/预案/规范改进）。

- "先止血后定位"的意识（限流/回滚/切流，而不是线上调试）
- 定位有证据链（哪个指标/日志/trace 锁定的）
- 复盘不甩锅，产出可落地的改进项（且确实落地了）

**解析：**
- 故障处理的全流程素养：止血 → 定位 → 修复 → 复盘（顺序不能乱）
- 复盘文化：是否沉淀了防复发机制（资深分水岭）


### ProjectGeneral-001-042 | ★★★★☆

项目的监控告警体系是怎么建的？

**答案：**
三层指标（系统资源→应用黄金指标 QPS/RT/错误率→业务核心指标 下单量/成功率）→ 告警规则设计（阈值/同比环比/持续时间，分级路由）→ 告警治理（误报率、值班响应 SLA）→ 从故障倒推补盲区。

- 业务指标是最灵敏的"烟雾报警器"（技术指标没崩但业务量掉了要先响）
- 告警分级与值班路由（P0 电话、P1 IM）
- 有告警治理动作（合并/降噪/消灭无效告警的具体案例）

**解析：**
- 指标分层（系统/应用/业务）与告警治理（别让人麻木）
- 黄金指标与业务指标的结合


### ProjectGeneral-001-043 | ★★★★☆

系统的稳定性预案有哪些？大促/高峰前做什么准备？

**答案：**
容量准备（扩容与压测）→ 依赖梳理（强弱依赖标记、降级开关清单）→ 预案清单（限流阈值/降级开关/熔断兜底，谁能操作、怎么验证生效）→ 演练与值班安排 → 复盘机制。

- 降级开关有"平时演练"（不是大促才第一次拉）
- 强弱依赖分级清单（哪些挂了必须止血、哪些无感）
- 值班 SOP（升级路径、决策人）

**解析：**
- 预案是"文档"还是"演练过的开关"
- 容量、依赖、降级、值班四位的准备完整度


### ProjectGeneral-001-044 | ★★★★☆

你值班时最害怕什么？预案都演练过吗？

**答案：**
Top 风险清单（单点依赖/数据不一致/发布事故/流量突增）→ 每项的预案与演练状态（一键切换/回滚时限）→ 值班 SOP（先止血后定位、升级路径、决策授权）→ 从预案演练中修过的坑。

说出具体演练发现（如"主备切换脚本半年没跑，真演练时 DNS 没生效"）；对"止血优先"有本能反应。

**解析：**
SRE 意识；预案从"文档"到"肌肉记忆"。


### ProjectGeneral-001-045 | ★★★★☆

你的系统如果用户量涨 10 倍，哪里先崩？怎么办？

**答案：**
按层推演崩塌顺序（接入→服务→缓存→DB→下游第三方）→ 每一层的当前水位与天花板 → 10 倍时的对应动作（水平扩容/读写分离/分片/降级）→ 最难啃的一块与长期方案。

- 推演有据：当前峰值水位、各组件上限数字
- 有序崩塌判断：通常缓存与无状态服务好扩，DB 与强状态组件先崩
- 承认长期瓶颈（如单写库），给出分片预案但克制启动条件

**解析：**
- 对自己系统瓶颈的清醒认知（按序崩塌点推演）
- 扩容预案的层次与触发条件


### ProjectGeneral-001-046 | ★★★★☆

如果重新做这个项目，你会改哪些设计？

**答案：**
选 2~3 个具体改点 → 每个改点：当时约束（为什么当初这么做是对的/无奈的）→ 现在的更优方案 → 迁移成本评估 → 已经推动的部分。

- 改点具体（"把 X 从同步改事件化""提前引入契约测试"），不是泛泛的"更规范"
- 体现"当时合理"的公允判断（技术债是有价值的杠杆）
- 有落地行动（至少一项已在做）而不是空想

**解析：**
- 是否有真实的架构反思（而不是"都挺好"）
- 反思是否基于"现在掌握的信息"合理（不以结果苛责当时）


### ProjectGeneral-001-047 | ★★★★☆

面试官反问环节：你应该问面试官什么问题？

**答案：**
三类问题按场合选：①团队与技术（技术栈演进方向、团队如何做技术决策、上线节奏）→ 展示工程素养；②成长（新人如何上手、导师制、考核维度）→ 展示长期意愿；③业务（产品阶段、竞争格局）→ 展示业务思维。避免首轮问薪资加班（HR 环节再谈）。

- 至少一个问题与面试内容呼应（"刚才聊到你们的缓存一致性，想了解实际采用的方案是？"）
- 银行/央国企语境：可问培养体系、轮岗、科技投入方向
- 问而不审：语气是请教不是挑战

**解析：**
- 反问质量反映候选人的思考层次与入职动机（这题出现在题库是因为"你有什么想问的"本身是考察点）


### ProjectGeneral-001-048 | ★★★★☆

面试官问"你还有什么想问的"：技术面的三个高质量反问

**答案：**
①团队挑战型："团队当前最大的技术挑战/正在推进的技术演进是什么？"（判断技术空间）②工程文化型："你们的发布节奏与代码评审/测试门禁是怎么运作的？"（判断工程成熟度）③成长型："新人前三个月的期望与上手路径？导师制怎么运作？"（判断培养体系）。禁区：首轮问薪资加班、问官网能查到的问题。

结合面试内容追问（"刚才聊到贵行的【X】系统，它和【Y】是怎么协同的？"）——证明在倾听且真感兴趣。

**解析：**
反问质量=思考层次与入职动机（与 P-SYS-003 呼应，此处给出可直接使用的版本）。

